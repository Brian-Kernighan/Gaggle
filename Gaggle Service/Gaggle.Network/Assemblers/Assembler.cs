using GaggleService.Gaggle.Objects;

namespace GaggleService.Gaggle.Network.Assemblers
{
    public static class Assembler
    {
        public static byte[] Assembly(User user)
        {
            return new byte[1];
        }
    }
}
