namespace GaggleService.Storage.Repositories.Account
{
    public interface IAccountRepository
    {
        //T CreateAccount(T entity);
        //bool TryGetAccountById(Guid id, out IAccountData entity);
        //bool TryGetAccountByEmail(string name, out IAccountData entity);
        //IAccountData GetAccountById(Guid id);
        //IAccountData GetAccountByEmail(string name);
        //Task<T> CreateAccountAsync(T entity, CancellationToken token = default(CancellationToken));
        //Task<IAccountData> GetAccountByIdAsync(Guid id, CancellationToken token = default(CancellationToken)); 
        //Task<IAccountData> GetAccountByNameAsync(string name, CancellationToken token = default(CancellationToken));
    }
}
